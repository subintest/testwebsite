
deployment:
  production:
    branch: [master]
    commands:
      - /bin/true  
machine:
  java:
    version: oraclejdk8
dependencies:
  pre:
    - sudo pip install html5validator
test:
  override:
    - html5validator --root ./

